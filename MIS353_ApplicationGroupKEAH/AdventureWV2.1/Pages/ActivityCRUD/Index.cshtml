@page
@model AdventureWV2._1.Pages.ActivityCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Activity[0].Aname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activity[0].Lid)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Activity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Aname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lid)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Aid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Aid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Aid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
