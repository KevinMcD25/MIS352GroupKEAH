@page
@model AdventureWV2._1.Pages.LandmarkCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Landmark[0].Lname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Landmark[0].Ltype)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Landmark) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ltype)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Lid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Lid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Lid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
